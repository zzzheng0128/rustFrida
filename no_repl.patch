diff --git a/rust_frida/src/args.rs b/rust_frida/src/args.rs
index abc..def 100644
--- a/rust_frida/src/args.rs
+++ b/rust_frida/src/args.rs
@@ -147,6 +147,10 @@ pub(crate) struct Args {
     #[arg(long = "server", conflicts_with_all = ["pid", "watch_so", "name", "spawn"])]
     pub(crate) server: bool,
 
+    /// 不进入 REPL，注入/加载脚本后保持运行，直到目标进程退出或收到信号
+    #[arg(long = "no-repl")]
+    pub(crate) no_repl: bool,
+
     /// 启动 HTTP RPC 服务器，暴露 agent 端 `rpc.exports` 注册的方法。
     ///
     /// 格式: --rpc-port <PORT> 或 --rpc-port <HOST:PORT>（默认绑定 0.0.0.0）。

diff --git a/rust_frida/src/main.rs b/rust_frida/src/main.rs
index abc..def 100644
--- a/rust_frida/src/main.rs
+++ b/rust_frida/src/main.rs
@@ -485,6 +485,28 @@ fn main() {
     rl.set_helper(Some(CommandCompleter::new()));
     let _ = rl.load_history(".rustfrida_history");
     println!("  {DIM}输入 help 查看命令，exit 退出{RESET}");
+    
+    // --no-repl: 跳过交互式 REPL，保持运行直到目标进程退出或收到信号
+    if args.no_repl {
+        log_info!("--no-repl 模式: 跳过 REPL，等待目标进程退出或 agent 断开...");
+        loop {
+            if session.disconnected.load(Ordering::Acquire) {
+                log_info!("agent 已断开，退出 --no-repl 模式");
+                break;
+            }
+            if args.spawn.is_some() && spawn::signal_received() {
+                log_info!("收到终止信号，退出 --no-repl 模式...");
+                send_shutdown(&session);
+                break;
+            }
+            std::thread::sleep(std::time::Duration::from_secs(1));
+        }
+        let _ = rl.save_history(".rustfrida_history");
+        // 跳过后面的 REPL 循环
+    } else {
+    // 原始 REPL 循环开始
 
     // 发送 shutdown 到 agent，随后等待 agent 完整清理并主动关闭 socket
     let send_shutdown = |s: &Session| {
@@ -627,6 +649,7 @@ fn main() {
     }
 
     let _ = rl.save_history(".rustfrida_history");
+    } // end if !args.no_repl
 
     // 等待 agent 完成清理并主动关闭 socket。交互路径不能无限等：
     // 超时后直接返回，并跳过远端 loader 残留清理，避免打断仍在清理中的 agent。
